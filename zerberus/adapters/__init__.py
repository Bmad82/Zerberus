"""Patch 174 — Transport-Adapter (Phase E).

Konkrete ``TransportAdapter``-Implementierungen aus P173. Ein Adapter
uebersetzt zwischen einem Transport (Telegram, Nala, Rosa-Internal) und
dem Message-Bus (``zerberus.core.message_bus``).
"""
